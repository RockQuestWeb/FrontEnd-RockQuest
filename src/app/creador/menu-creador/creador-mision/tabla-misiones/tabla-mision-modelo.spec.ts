import { TablaMisionModelo } from './tabla-mision-modelo';

describe('TablaMisionModelo', () => {
  it('should create an instance', () => {
    expect(new TablaMisionModelo()).toBeTruthy();
  });
});
